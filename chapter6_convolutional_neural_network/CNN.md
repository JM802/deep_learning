### 卷积神经网络

1. 核心设计：

   - 降低分辨率：通过卷积层和pooling层，不断减少特征图尺寸，减少计算量，进行特征压缩
   - 增加通道数：通道数代表特征，增加通道数即增加模型的特征，用大量通道全方位表示图片特征
2. 池化层：通过池化层，增强模型鲁棒性

   - 降维：通过池化层降低图片分辨率。减少计算量
   - 平移不变性：对微小旋转、缩放等不敏感，增强鲁棒性
3. 模型通道数：

   - 低层通道：代表低级特征
   - 高层通道：代表高级特征，增加通道数让模型能学习到更多特征

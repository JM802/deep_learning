# core component

1. data:we need high quality data
2. model:activation function(non-linear)
3. loss function:define how we calculte the error(regression:MSE  classfication:Cross-Entropy Loss)
4. optimization algorithm:The strategy to update parameters (e.g., SGD, Adam). It uses **Gradients** to find the direction that minimizes the loss.

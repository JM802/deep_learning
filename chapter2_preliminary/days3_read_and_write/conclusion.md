# data processing

When some data is missing(NaN),we have two approaches to handle it:

1. impute(补全) values:select a value(typically mean) to replace NaN
2. delete records:exclude the rows containing the NaN values

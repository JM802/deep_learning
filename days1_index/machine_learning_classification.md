# machine learning classification

## 一、supervised learning

1. regression:the input decided,the output is a continuous value
2. classifaction:Mapping input **$x$** to a **discrete category** (class),**Output:** A probability distribution over **$N$** classes .
3. object detection：dectction each object of the image
4. ranking:Learning the **relative order** of items.
5. sequence to sequence：input the context(text,audio),use comtext output precise consequence

## 二、unsupervised learning

1. clustering：when we have no label,how we classification
2. principal component analysis:how we find the relative of different object
3. causality:find the causality of multiple data
4. generative adversarial networks(GAN):synthetic data

## 三、reinforcement learning

observation the enviroment,decide the action,get reward and penalty
